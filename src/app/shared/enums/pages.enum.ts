/**
 * @description Used for setting current page per route change event.
 */
export enum Pages {
    home = 'Home',
    about = 'About',
    portfolio = 'portfolio',
    service = 'services',
    contact = 'contact'
}